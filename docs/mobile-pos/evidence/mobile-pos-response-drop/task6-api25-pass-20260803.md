# Task 6 corrected lost-response staging evidence

- Protocol: `mobile-pos-response-drop/v1`
- Operator evidence ID: `task6-api25-pass-20260803`
- Timestamp: `2026-08-03T14:59:29+07:00`
- Operator: Mobile POS staging operator / repository maintainer
- Device: `mobile-pos-api25` (`emulator-5554`, Android 7.1.1 / API 25)
- Android commit: `acd6769ced033c91799bb0f9b9846fc884ce7dea`
- APK SHA-256: `19e889569692608ed3d568529c39780555f6823abfe867a8a8a54ddb90957504`
- Backend runtime authority: `40d2f2b56c6aa92b363485487e58ccb3a62e334c`
- Backend documentation/main SHA: `38728df32394aac1fc5c49387b2ea6e0f3b5c15b`
- Staging site: `task9-staging.localhost`
- POS Profile: `Task 9 Mobile POS`
- Original request UUID: `f462fe32-fe43-4912-8722-16bee4762c42`
- Replay request UUID: `f462fe32-fe43-4912-8722-16bee4762c42`
- Persisted request-body SHA-256: `032e64554d5fbba44a96ce6a0ab95e1e7e1a0fa42fbc7e7954a445b081f57478`
- Replay request-body SHA-256: `032e64554d5fbba44a96ce6a0ab95e1e7e1a0fa42fbc7e7954a445b081f57478`
- Resulting POS Opening Entry: `POS-OPE-2026-00007`

## Original attempt and fault removal

The fresh staging-only proxy began in `WAITING_CAPTURE` with no prior UUID or body hash and `fault_active=false`. It bound to the authenticated dedicated API 25 staging session without recording credentials. The operator submitted exactly one opening request. The proxy captured its UUID and request-body hash before upstream transmission and armed the fault for that exact pair.

Backend processing completed with HTTP 201 and committed one submitted, Open POS Opening Entry. The proxy confirmed that commit and disabled the one-shot fault before withholding delivery of the completed response. The completed original response was then dropped.

Sanitized final proxy facts:

```text
phase=REPLAY_COMPLETED
fault_active=false
original_upstream_status=201
original_response_dropped=true
backend_reference=POS-OPE-2026-00007
backend_count=1
```

## Replay and reconciliation

The durable recovery worker replayed the exact persisted logical mutation. Ingress observed the same UUID and the same request-body SHA-256. The replay reached the backend only after the fault was disabled and completed with HTTP 200.

```text
uuid_match=true
body_hash_match=true
replay_upstream_status=200
sessions_current_after_replay=1
bootstrap_refresh_after_replay=1
```

Android remained alive after the recovered opening completed. Logcat from the original request timestamp onward contained no `FATAL EXCEPTION`, `NetworkOnMainThreadException`, or application-process-death event. This does not rewrite the prior failed run; its separate evidence remains authoritative for the earlier crash.

## Authoritative exactly-one result

Before cleanup, the targeted backend queries returned:

```text
logical_count=1
entries=POS-OPE-2026-00007
statuses=1:Open
profile_count=1
user_count=1
request_count=1
request_status=Completed
request_reference=POS-OPE-2026-00007
request_endpoint=v1.sessions.open
request_hash_count=1
```

## Targeted cleanup

Before cleanup, the operator verified zero POS Closing Entry references, zero later POS Invoice records for the test cashier/profile, zero POS Invoice records carrying the run UUID, zero amended POS Opening Entry references, and exactly one matching Mobile POS Request.

Only `POS-OPE-2026-00007` and its exact Mobile POS Request were targeted through Frappe document APIs. The Opening Entry was cancelled, the exact Mobile POS Request was deleted, the cancelled Opening Entry was deleted, and the staging transaction was committed. Dedicated API 25 application and test-package state was cleared. The one-shot proxy was stopped.

Post-cleanup verification:

```text
opening_count_for_uuid=0
mobile_pos_request_count_for_uuid=0
active_opening_count_for_test_cashier_and_profile=0
proxy_listener_count=0
```

- Final conclusion: `PASS` — post-commit response loss replayed the exact persisted UUID/body, produced exactly one backend logical result, reconciled `sessions.current` exactly once, refreshed capabilities exactly once, and caused no new application crash.
